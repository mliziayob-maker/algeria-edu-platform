# العمارة التقنية للمنصة

## نظرة عامة
تم بناء المنصة باستخدام بنية **Jamstack** مع فصل تام بين الواجهة والخادم وقاعدة البيانات.

## المكونات الرئيسية

### 1. الواجهة الأمامية (Frontend)
- **التقنية**: Next.js 14 (مع App Router)
- **اللغة**: TypeScript
- **التصميم**: Tailwind CSS
- **إدارة الحالة**: Zustand
- **النماذج**: React Hook Form

### 2. الخادم (Backend)
- **التقنية**: Supabase Edge Functions (بدون خادم)
- **اللغة**: TypeScript
- **الوظائف**:
  - المصادقة (Auth)
  - معالجة الذكاء الاصطناعي
  - إدارة المحتوى

### 3. قاعدة البيانات
- **النظام**: PostgreSQL (عبر Supabase)
- **نموذج البيانات**: علائقي
- **الميزات**: RLS (أمان على مستوى الصف)، Real-time subscriptions

### 4. الذكاء الاصطناعي
- **النموذج**: Google Gemini API
- **آلية العمل**: RAG (Retrieval-Augmented Generation)
- **التخزين المؤقت**: لتقليل التكلفة والاستجابة السريعة

## تدفق البيانات
1. المستخدم يتفاعل مع الواجهة.
2. تُرسل الطلبات إلى Edge Functions.
3. تتعامل الوظائف مع قاعدة البيانات و/أو API الذكاء الاصطناعي.
4. تُعاد النتائج إلى الواجهة في الوقت الفعلي.

## الأمان
- **المصادقة**: JWT عبر Supabase Auth.
- **التفويض**: سياسات RLS على مستوى الصف.
- **البيانات الحساسة**: مشفرة في النقل (HTTPS) والسكون (في Supabase).

## النشر
- **الواجهة**: Vercel (نشر تلقائي من فرع main).
- **قاعدة البيانات والخادم**: Supabase (سحابي).
- **CDN**: Vercel Edge Network.
